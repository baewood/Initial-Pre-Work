// I learned how to write arrow functions with parameters. I also alearned that you can pass more than
// one arguement into arrow functions. 

const myConcat = (arr1, arr2) => {
  "use strict";
  return arr1.concat(arr2);
};
// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
