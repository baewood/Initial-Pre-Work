// In this exercise, I learned about the inequality operator !=. It returns false where equality would return true.
// It will convert data types of values while comparing.

// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(29);
