// In this lesson, I learned howt o return Boolean values from functions.
// This exercises used comparison operators to change what was in the original if/else
// statement into a return.

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
