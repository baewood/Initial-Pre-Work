// In this lesson, I learned that there are many comparison operators in JavaScript. They all return a Boolean
// value. The most basic operator is the equality opterator ==, which we use in this exercise.

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
