// I learned how to use the parseInt Function and what it does. parseInt() parases a string and returns an integer.

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
