// I learned how to used the parseInt function with a Radix. I also learned that a Radix is
// an integer between 2 and 36. In this exercise, I used 2.

function convertToInteger(str) {
  return parseInt(str, 2);
}

convertToInteger("10011");
