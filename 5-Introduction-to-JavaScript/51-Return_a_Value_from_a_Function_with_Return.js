// In this lesson, I learned how to return a value from a function with return.
// Once values are passed into a function with arguments, the return statement sends a
// value back out of the function. The console.log checks the function and shows the answer in the dev console.

function timesFive(num) {
  return num * 5;
}

console.log(timesFive(5));
