// I learned how to generate random fractions with JavaScript using Math.random.

function randomFraction() {

  // Only change code below this line.

  var num = 0;
  while (num === 0){
    num = Math.random();
  }
  return num;

  // Only change code above this line.
}
