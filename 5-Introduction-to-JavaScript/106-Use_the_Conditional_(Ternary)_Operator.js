// I learned how to use the condition (ternary) operator. Instead of using if/else statements,
// they are condensed to one line and used as the return value.

function checkEqual(a, b) {
  return a === b ? true : false;
}

checkEqual(1, 2);
